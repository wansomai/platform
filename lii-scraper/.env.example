# API key that callers must send in the X-Api-Key header.
# Generate a strong random value, e.g.: openssl rand -hex 32
LII_API_KEY=replace_with_a_strong_random_secret

# Port the gunicorn service listens on (nginx proxies to this)
PORT=8000
